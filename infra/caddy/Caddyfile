{$APP_DOMAIN:localhost} {
  encode zstd gzip

  @backend path /health /auth* /admin* /tournaments* /rounds*
  handle @backend {
    reverse_proxy backend:4000
  }

  handle {
    reverse_proxy frontend:80
  }
}
