{$APP_DOMAIN:localhost} {
  encode zstd gzip

  @backend path /health /auth* /admin* /tournaments* /teams* /rounds* /dashboard* /profile* /announcements* /messages* /notifications* /platform* /uploads*
  handle @backend {
    reverse_proxy backend:4000
  }

  handle {
    reverse_proxy frontend:80
  }
}
